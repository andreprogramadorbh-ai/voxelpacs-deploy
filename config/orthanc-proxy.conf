# =============================================================================
# VOXEL PACS — config/orthanc-proxy.conf
# Configuração de proxy para o Orthanc (gerada pelo install.sh)
# Este arquivo é um template de referência
# =============================================================================

# Proxy DICOMweb → Orthanc
# location /dicom-web/ {
#     proxy_pass         http://ORTHANC_HOST:ORTHANC_PORT/dicom-web/;
#     proxy_set_header   Authorization "Basic BASE64_CREDENTIALS";
#     proxy_set_header   Host $host;
#     proxy_read_timeout 300s;
#     add_header         Access-Control-Allow-Origin "*" always;
#     add_header         Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
#     add_header         Access-Control-Allow-Headers "Authorization, Content-Type" always;
# }

# Proxy WADO → Orthanc
# location /wado {
#     proxy_pass         http://ORTHANC_HOST:ORTHANC_PORT/wado;
#     proxy_set_header   Authorization "Basic BASE64_CREDENTIALS";
#     proxy_set_header   Host $host;
# }
