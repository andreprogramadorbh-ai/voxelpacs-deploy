FROM nginx:alpine

# Remover configuração padrão
RUN rm /etc/nginx/conf.d/default.conf

# Instalar curl para healthcheck
RUN apk add --no-cache curl

# As configurações serão montadas via volume no docker-compose
