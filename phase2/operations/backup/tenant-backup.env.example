# Instalar como /etc/voxelpacs-backup/tenants/<tenant>.env com modo 0600.
# O namespace deve ser único por tenant; nunca compartilhar tags entre clientes.
BACKUP_ENABLED=false
BACKUP_NAMESPACE=cliente-b
RETENTION_DAYS=30
RETENTION_PRUNE_APPROVED=false
RPO_HOURS=24
RESTORE_TEST_INTERVAL_DAYS=90
